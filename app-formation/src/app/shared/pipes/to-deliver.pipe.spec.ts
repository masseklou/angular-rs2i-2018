import { ToDeliverPipe } from './to-deliver.pipe';

describe('ToDeliverPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDeliverPipe();
    expect(pipe).toBeTruthy();
  });
});
